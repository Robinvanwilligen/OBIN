<%= form_for(@categories_location) do |f| %>
  <% if @categories_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_location.errors.count, "error") %> prohibited this categories_location from being saved:</h2>

      <ul>
      <% @categories_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br />
    <%= f.number_field :location_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
