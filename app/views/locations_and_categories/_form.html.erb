<%= form_for(@locations_and_category) do |f| %>
  <% if @locations_and_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locations_and_category.errors.count, "error") %> prohibited this locations_and_category from being saved:</h2>

      <ul>
      <% @locations_and_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
